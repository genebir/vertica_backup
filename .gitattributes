# 기본: 텍스트 파일은 저장소에 LF 로 정규화
* text=auto eol=lf

# 실행 스크립트는 반드시 LF (CRLF 면 Linux/RHEL 에서 깨짐)
*.sh   text eol=lf
*.py   text eol=lf
*.yaml text eol=lf
*.md   text eol=lf
Dockerfile text eol=lf

# 바이너리는 변환/diff 금지
*.tar.gz binary
*.tar    binary
*.whl    binary
